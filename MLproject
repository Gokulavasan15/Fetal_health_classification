name: Fetal_health_classification

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      n_estimators: {int}
      max_depth: {nt}
    command: "python test1.py {n_estimators} {max_depth}"
